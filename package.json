{
  "name": "sekirei-todo",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "description": "",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "dev": "docker compose watch",
    "stop": "docker compose stop",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "db:migrate": "pnpm --filter database exec sh -c 'set -a && . ../.env.local && drizzle-kit push --force'",
    "db:seed": "pnpm --filter database exec sh -c 'set -a && . ../.env.local && tsx addTestData.ts'"
  }
}
